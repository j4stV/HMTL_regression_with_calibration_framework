# Small regression model for an installation check, not a benchmark.
encoder:
  hidden_width: 32
  alpha_dropout: 0.01
  residual: true
hmtl:
  enabled: true
  aux_task: bins
  low_layer: 2
  high_layer: 4
  lambda_aux: 0.2
  n_bins: 5
  proj_dim: 16
